<?php

namespace Leaf\Inertia;

/**
 * Marker for props that are excluded from the initial page load
 * and only evaluated when explicitly requested in a partial reload.
 */
interface IgnoreFirstLoad
{
}
